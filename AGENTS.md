# AGENTS.md

## Cursor Cloud specific instructions

This is an empty starter repository ("Testing cursor's Cloud Agent") containing only a `README.md` and `LICENSE` file. There is no application code, no dependency files, no services, and no build/test/lint configuration.

### Current state

- **No package manager lockfiles or dependency manifests** — nothing to install.
- **No services to start** — no backend, frontend, database, or other runnable components.
- **No tests, lint, or build commands** — none are configured.

### When application code is added

Once code is committed, update this section with:
- How to install dependencies (e.g. `npm install`, `pip install -r requirements.txt`).
- How to run lint, tests, and the dev server.
- Any non-obvious setup caveats discovered during development.
